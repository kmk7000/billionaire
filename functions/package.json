{
  "name": "billionaire-functions",
  "description": "OCR API for the Billionaire app (see src/index.ts for why it must be deployed)",
  "type": "module",
  "main": "lib/index.js",
  "engines": { "node": "22" },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "@google/genai": "^1.29.0",
    "express": "^4.21.2",
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^6.6.0"
  },
  "devDependencies": {
    "typescript": "~5.8.2",
    "@types/express": "^4.17.21"
  },
  "private": true
}
