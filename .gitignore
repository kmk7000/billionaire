node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
service-account.json

# Local-only third-party design skill (npm install -g ui-ux-pro-max-cli && uipro init --ai claude)
.claude/skills/ui-ux-pro-max/

# Operator console build output (deployed separately from the app)
dist-admin

# Cloud Functions build output
functions/lib/
